# AndroidX is required by the instrumentation runner and androidx.test
# dependencies. Keep it explicit so clean CI/consumer-independent builds do
# not depend on a developer's global ~/.gradle/gradle.properties.
android.useAndroidX=true
